import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    padding: 15,
    width: 350,
    backgroundColor: '#FEFEFF',
    borderRadius: 8,
    alignItems: 'center',
    marginBottom: 20,
    fontWeight: 'normal',
    fontSize: 16,
  },
});
